import { styled } from '@mui/material';

export const GeneralPageStyle = styled('div')(() => ({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  width: '100vw',
  height: '92vh',
  overflow: 'hidden',
}));
